import {
    StyleSheet
} from 'react-native';

import {
    Colors
} from 'react-native/Libraries/NewAppScreen';

export const styles = StyleSheet.create({
    menuButtonWrapper: {
        position: "absolute",
        left: 0
    },
    Layout:{
        top: 24,
    }
});

